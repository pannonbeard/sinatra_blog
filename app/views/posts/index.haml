%a{href: '/posts/new', role: 'button' } New Post

%table
  %thead
    %tr 
      %td Title
      %td Content
  %tbody
  - @posts.each do |post|
    %tr
      %td
        %a{ href: "/posts/#{post.id}" }= post.title
      %td= post.content

